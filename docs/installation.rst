============
Installation
============

At the command line::

    $ easy_install django-ftl

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-ftl
    $ pip install django-ftl
